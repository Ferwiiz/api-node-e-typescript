{
    "compilerOptions": {
      "target": "ES6",                      // Define a versão do JavaScript para a qual o código será compilado
      "module": "commonjs",                 // Define o sistema de módulos a ser usado (para Node.js)
      "outDir": "./dist",                   // Diretório de saída para os arquivos JavaScript compilados
      "rootDir": "./src",                   // Diretório raiz onde os arquivos TypeScript estão localizados
      "strict": true,                       // Habilita todas as verificações de tipo estritas
      "esModuleInterop": true,              // Habilita interoperabilidade entre módulos CommonJS e ES
      "skipLibCheck": true,                 // Ignora checagem de tipos em arquivos de declaração (.d.ts)
      "forceConsistentCasingInFileNames": true // Garante que os nomes de arquivos sejam consistentes com a capitalização
    },
    "include": ["src/**/*"],                // Inclui todos os arquivos TypeScript no diretório src
    "exclude": ["node_modules"]             // Exclui o diretório node_modules
  }
  